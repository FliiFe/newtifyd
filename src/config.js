import {levels as l} from 'loglevel'

export default {
    height: 300,
    width: 370,
    monitor: 0,
    devtools: false,
    serverPort: 3113,
    devServer: false,
    loglevel: l.DEBUG, // Possible values are DEBUG, INFO, WARN, ERROR
}
